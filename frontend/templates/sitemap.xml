<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <url><loc>{{ site.site_url }}/</loc></url>
  <url><loc>{{ site.site_url }}/blog/</loc></url>
  {% for post in posts %}
  <url><loc>{{ site.site_url }}/blog/{{ post.slug }}/</loc></url>
  {% endfor %}
</urlset>
