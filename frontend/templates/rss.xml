<?xml version="1.0" encoding="UTF-8" ?>
<rss version="2.0">
  <channel>
    <title>{{ site.site_name }}</title>
    <link>{{ site.site_url }}</link>
    <description>{{ site.description }}</description>
    {% for post in posts %}
    <item>
      <title>{{ post.title }}</title>
      <link>{{ site.site_url }}/blog/{{ post.slug }}/</link>
      <guid>{{ site.site_url }}/blog/{{ post.slug }}/</guid>
      <pubDate>{{ post.date.strftime('%a, %d %b %Y 00:00:00 +0000') }}</pubDate>
      <description><![CDATA[{{ post.body_html }}]]></description>
    </item>
    {% endfor %}
  </channel>
</rss>
