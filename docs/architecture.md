# Architecture

- **Architecture type:** static site generator + optional static hosting deploy command.
- **Rendering:** Markdown + Jinja2 templates.
- **CLI:** Typer command set (`blog new`, `blog dev`, `blog build`, `blog deploy`).
- **Output:** static files in `dist/` with RSS + sitemap.
- **Frontend style:** single serif stylesheet with ~700px reading width.
